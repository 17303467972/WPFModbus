<Window x:Class="ModbusTest.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:zyy="clr-namespace:ModbusTest.Components"
        mc:Ignorable="d"
        FontFamily="Microsoft YaHei" FontSize="12" FontWeight="ExtraLight"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Title="MainWindow" Height="650" Width="1200">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="1" CaptionHeight="50"></WindowChrome>
    </WindowChrome.WindowChrome>
    
    <Window.Background>
        <RadialGradientBrush Center="0.5,0" GradientOrigin="0.5,-0.5" RadiusX="0.7" RadiusY="1.2">
            <GradientStop Color="#FF61BAFF" Offset="0"></GradientStop>
            <GradientStop Color="#FF29384E" Offset="1"></GradientStop>
        </RadialGradientBrush>
    </Window.Background>
    
    <Window.Resources>
        
        <Style TargetType="ToggleButton" x:Key="SwitchButtonStyle">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="#9FFF"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                       <Border Background="#1000" CornerRadius="5" Name="root">
                           <Grid>
                               <Grid.RowDefinitions>
                                   <RowDefinition/>
                                   <RowDefinition Height="18"/>
                               </Grid.RowDefinitions>
                               <Border Height="17" Width="33" CornerRadius="8.5" Background="#fff" Name="back">
                                   <Grid>
                                       <TextBlock Text="开" FontFamily="9" Foreground="White"
                                                  VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5,0"/>
                                       <TextBlock Text="关" FontFamily="9" Foreground="Gray"
                                                  VerticalAlignment="Center" HorizontalAlignment="Right" Margin="5,0"/>
                                       <Ellipse Width="15" Height="15" Fill="#DDD"
                                                Margin="1,0" HorizontalAlignment="Left" Name="ellipse"/>

                                   </Grid>
                               </Border>
                               <ContentPresenter Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                           </Grid>
                       </Border>
                        
                        <ControlTemplate.Triggers>
                           <Trigger Property="IsMouseOver" Value="True">
                               <Setter TargetName="root" Property="Background" Value="#2000"></Setter>
                           </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="back" Property="Background" Value="Orange"/>
                                <Setter TargetName="ellipse" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="ellipse" Property="Fill" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="Button" x:Key="ControlButtonStyle">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="#1000"/>
            <Setter Property="Foreground" Value="#9FFF"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <Border Background="Transparent" Name="bor" CornerRadius="5">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bor" Property="Background" Value="#2000"></Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Window.Triggers>
        <EventTrigger RoutedEvent="Button.Click" SourceName="btnConfig">
            <BeginStoryboard>
                <Storyboard>
                    <ThicknessAnimation To="0,0,0,0" Duration="0:0:0.3"
                                        Storyboard.TargetName="bor_config"
                                        Storyboard.TargetProperty="Margin"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
        <EventTrigger RoutedEvent="Button.Click" SourceName="btnConfigClose">
            <BeginStoryboard>
                <Storyboard>
                    <ThicknessAnimation To="0,0,-250,0" Duration="0:0:0.3"
                                        Storyboard.TargetName="bor_config"
                                        Storyboard.TargetProperty="Margin"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
   
    <Grid>
       
        <Grid Margin="20,0">
            <Grid.Effect>
                <BlurEffect Radius="{Binding BlurRadius}"/>
            </Grid.Effect>
            <Grid.RowDefinitions>
                <RowDefinition Height="55"/>
                <RowDefinition Height="30"/>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <!--第一行信息-->
            <Border Height="1" VerticalAlignment="Bottom">
                <Border.Background>
                    <RadialGradientBrush RadiusX="0.7">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="#0FFFFFFF" Offset="0.3"/>
                        <GradientStop Color="#0FFF" Offset="1"/>
                    </RadialGradientBrush>
                </Border.Background>
            </Border>
            
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="串口通信监控" Foreground="#DD87E8EC" FontSize="13"
                           FontWeight="Normal" VerticalAlignment="Center" Margin="10,0"/>
            </StackPanel>
            
            <TextBlock Text="串口通信监控助手" VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="18" FontWeight="Normal">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="Gold" Offset="0"/>
                        <GradientStop Color="Orange" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                <ToggleButton Content="监控" Style="{StaticResource SwitchButtonStyle}" 
                              IsChecked="{Binding IsStart}"/>
                <ToggleButton Content="日志" Style="{StaticResource SwitchButtonStyle}" 
                              IsChecked="{Binding IsLog}"/>
                <Button Style="{StaticResource ControlButtonStyle}" Name="btnConfig"
                        Command="{Binding ConfigCommand}">
                    <StackPanel>
                        <TextBlock Text="^" HorizontalAlignment="Center"/>
                        <TextBlock Text="设置" HorizontalAlignment="Center" />
                    </StackPanel>
                </Button>
               
                <Button Style="{StaticResource ControlButtonStyle}" Click="ButtonBase_OnClick">
                    <StackPanel>
                        <TextBlock Text=">" HorizontalAlignment="Center"/>
                        <TextBlock Text="退出" HorizontalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
            
            
            <!--第二行信息-->
           <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
               <TextBlock Text="时间" FontSize="14" Foreground="Gold"/>
               <TextBlock Text="{Binding CurrentDate,StringFormat={}{0:yyyy年MM月dd日 dddd},ConverterCulture=zh-CN}" VerticalAlignment="Center" Margin="10,0" Foreground="#8FFF"></TextBlock>
           </StackPanel>
            
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right" Orientation="Horizontal" >
                <StackPanel.Effect>
                    <DropShadowEffect BlurRadius="5" Color="Gray" ShadowDepth="0" Opacity="0.3"></DropShadowEffect>
                </StackPanel.Effect>
                
                <TextBlock Text="!" FontSize="14" Foreground="{Binding MsgColor}"/>
                <TextBlock Text="{Binding Message}" VerticalAlignment="Center" Margin="10,0" Foreground="{Binding MsgColor}"/>
            </StackPanel>
            
            <!--第三行信息-->
            <ItemsControl ItemsSource="{Binding ItemList}" Grid.Row="2">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
                
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Border CornerRadius="5" Background="#1000" BorderThickness="0.5" Margin="10">
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#87E8EC" Offset="0"/>
                                    <GradientStop Color="Transparent" Offset="0.3"/>
                                    <GradientStop Color="Transparent" Offset="0.8"/>
                                    <GradientStop Color="#87E8EC" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            
                            <Grid Margin="10,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition/>
                                    <RowDefinition Height="40"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <!--从站编号-->
                                <TextBlock Text="{Binding SlaveId,StringFormat={}{0:00}--号从站}"
                                           VerticalAlignment="Center" Foreground="#9987E8EC" FontWeight="Normal"/>
                                <!--图表图例-->
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                            HorizontalAlignment="Right" Grid.Column="1">
                                    <Border Width="8" Height="8" Background="#87E8EC" Margin="5"/>
                                    <TextBlock Text="温度" Foreground="#9FFF" FontSize="11" Margin="0,0,5,0"/>
                                    <Border Width="8" Height="8" Background="Orange" Margin="5"/>
                                    <TextBlock Text="湿度" Foreground="#9FFF" FontSize="11" />
                                </StackPanel>
                                <!--温度显示区-->
                                <Border CornerRadius="5" Background="#1000" Grid.Row="1" Margin="0,0,0,5">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        
                                        <!--水印添加区-->
                                        
                                        <Border HorizontalAlignment="Center" VerticalAlignment="Top"
                                                Background="#BF90" CornerRadius="0,0,5,5">
                                            <TextBlock Text="温度" Margin="15,3" FontSize="11" Foreground="White"/>
                                        </Border>
                                        
                                        <zyy:Meter Grid.Row="1" Margin="5,20,5,0" />
                                    </Grid>
                                    
                                    
                                </Border>
                                
                            </Grid>
                        </Border>
                        
                    </Grid>
                </DataTemplate>
                    
            </ItemsControl.ItemTemplate>
                
            
            </ItemsControl>
            
            <!--第四行信息-->
            <TextBlock Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center"
                       Foreground="#3FFF" FontSize="11">
                <Run Text="by zyy"/>
                <Run Text=" "/>
                <Run Text="{Binding CurrentDate,StringFormat={}{0:yyyy}}"/>
            </TextBlock>
            
            
        </Grid>
        <!--配置信息界面-->
        <Border HorizontalAlignment="Right" Width="240" Background=" #DDF7F9FA" BorderBrush="#DDD"
                BorderThickness="1,0,0,0" Margin="0,0,-250,0" Name="bor_config">
            <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="45"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="35"/>
                <RowDefinition Height="60"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="参数设置" VerticalAlignment="Center" HorizontalAlignment="Center"
                       FontSize="18">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="10" Color="Gray" ShadowDepth="0" Opacity="0.4"/>
                </TextBlock.Effect>
            </TextBlock>
            
            <Button Style="{StaticResource ControlButtonStyle}" Width="33" Height="28"
                    VerticalAlignment="Center" HorizontalAlignment="Left" Background="Transparent"
                    Name="btnConfigClose" Command="{Binding ConfigCloseCommand}">
                <Path Data="M0 5,10 5M4 0 ,10 5,4 10" Stroke="Gray" StrokeThickness="1"/>
            </Button>
            <!--串口名称-->
            <Grid Grid.Row="1" Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="串口名称" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" 
                          VerticalAlignment="Center" Height="30"
                          ItemsSource="{Binding PortNameList}"/>
                
            </Grid>
            <!--波特率-->
            <Grid Grid.Row="2" Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="波特率" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" VerticalAlignment="Center" Height="30"
                          SelectedValue="{Binding BaudRate}"
                          ItemsSource="{Binding BaudRateList}"/>
            </Grid>
            
            <Grid Grid.Row="3" Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="数据位" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" 
                          VerticalAlignment="Center" Height="30"/>
            </Grid>
            <Grid Grid.Row="4" Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="停止位" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" 
                          VerticalAlignment="Center" Height="30"/>
            </Grid>
            <Grid Grid.Row="5" Margin="10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="校验位" VerticalAlignment="Center"/>
                <ComboBox Grid.Column="1" 
                          VerticalAlignment="Center" Height="30"/>
            </Grid>
            
            <TextBlock Grid.Row="6" Text="不建议改动"  TextWrapping="Wrap" LineHeight="20" Foreground="Red" 
                       VerticalAlignment="Center" HorizontalAlignment="Center" />
            
            <!--日志路径-->
            <Grid Grid.Row="7" Margin="10,0" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="日志路径" VerticalAlignment="Center"/>
                <Border Grid.Column="1" Background="White" BorderBrush="#EEE" BorderThickness="1"
                        CornerRadius="5" Height="33">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="30"/>
                        </Grid.ColumnDefinitions>
                        <TextBox BorderThickness="0" Background="Transparent"
                                 VerticalAlignment="Center" Text="{Binding LogPath}"/>
                        <Border Width="1" HorizontalAlignment="Right" Background="#EEE" Margin="0,4"/>
                        <Button Content="..." Grid.Column="1" Background="Transparent"
                                Style="{StaticResource ControlButtonStyle}" FontSize="18" 
                                Margin="2" Width="26" Foreground="Gray"
                                Command="{Binding LogPathSelectCommand}"/>
                    </Grid>
                    
                </Border>
            </Grid>
            
            <!--打开日志目录-->
            <TextBlock Grid.Row="8" VerticalAlignment="Top" HorizontalAlignment="Center"
                       Margin="0,5">
                <Hyperlink>打开日志目录</Hyperlink>
            </TextBlock>
            </Grid>
       
        </Border>


    </Grid>
</Window>
